"""D.A.R.E. — Desktop Action Recording & Execution.

A cross-platform pipeline that records desktop interactions, converts them
into robust semantic actions, validates them via simulation, and executes
them reliably across variable environments.

Modules are exposed both as a CLI and (from Phase 7) as MCP tools over an
agnostic stdio transport.
"""

__version__ = "0.1.0"
__all__ = ["__version__"]
